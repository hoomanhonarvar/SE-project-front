function ShowAndHidePassword(){
    const element = document.getElementById('password');
    if(element.type === 'password')
    {console.log(element.type = Text)}
    else{console.log(element.type = 'password')}
  }
  
export default ShowAndHidePassword;
